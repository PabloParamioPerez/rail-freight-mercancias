# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/
dist/
build/

# uv: uv.lock IS committed (this is an application/analysis repo, not a library).
# Do not add it here. Use `uv sync --frozen` for reproducible installs.

# Data: keep the tree, ignore heavy/raw contents (use DVC/manual sync)
data/raw/*
data/interim/*
data/processed/*
data/external/*
!data/**/.gitkeep

# Outputs
*.gpkg
*.duckdb
*.html
maps/
!docs/**/*.html

# Env / secrets
.env

# OS / editor
.DS_Store
